idap and aad (Azure AD) logins:
they are part of authenntication

User
  ↓
AAD / LDAP / GitHub / OIDC
  ↓
OpenShift OAuth Server
  ↓
OpenShift Cluster



找不着developer -> add+

ways of deployment: via UI, via terminal
1. add a yaml file straightaway
2. import from git (developer -> Add+ -> git repo, unavailable)

deploying with CLI:

3 types of users:
regular users
system users: to interact with infrastructure, some are created by default
service accounts

