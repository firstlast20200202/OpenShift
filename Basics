console.redhat.com/openshift
create cluster via one o the cloud platforms, in reality I wouldnt have access to do it since a cluster inlcudes everything like controlplane and worker nodes etc., whereas 
a project in openshift is more like a kubernetes namespace... so im assuming admin creates multiple namespaces and assign developers in different teams access to each namespace.



Section 4: Project and Users
ways of deployment: via UI, via terminal
1. add a yaml file straightaway
2. import from git (developer -> Add+ -> git repo, unavailable)

deploying with CLI:


command example:
oc get service    //oc is counterpart of kubectl for openshift



